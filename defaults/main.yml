# Standards: 0.1
---
# @var bill_user:description: User to run user-specific commands
# @var bill_user:value: bill_user | default(homebrew_user) | default(ansible_user_id)
# bill_user:

# @var bill_started:description: Restart app if already running
bill_started: True

...
